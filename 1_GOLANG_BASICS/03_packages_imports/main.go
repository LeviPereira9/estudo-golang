package main

// imports brings external packages into the file that u r working
// where u actually need it
import (
	"fmt"
	"math"
)

func main(){

	//packagesName.FunctionName -> call a function from a package

	fmt.Println("sqrt(25) = ", math.Sqrt(25))
	
}